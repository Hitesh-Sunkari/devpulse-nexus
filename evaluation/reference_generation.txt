DEVPULSE NEXUS - REFERENCE ANSWER GENERATION
======================================================================

SOURCE CODE / KNOWLEDGE BASE
======================================================================

================================================================================
FILE: main.py
================================================================================
from fastapi import FastAPI, Request
from fastapi.responses import HTMLResponse
from fastapi.templating import Jinja2Templates

import json
import platform
import time
from datetime import datetime

import psutil
import requests

from app.rag import retrieve_context
from app.orchestrator import orchestrate_request
from app.docker_monitor import get_docker_metrics
from app.telemetry_history import record_telemetry, get_history


# ============================================================
# DEVPULSE NEXUS
# AI-Powered Developer Environment Observability
# and AIDevOps Intelligence
# ============================================================

app = FastAPI(
    title="DevPulse Nexus",
    description=(
        "AI-powered developer environment observability, "
        "Digital Twin telemetry, Docker monitoring, and "
        "RAG-based troubleshooting."
    ),
    version="1.0.0",
)

templates = Jinja2Templates(directory="templates")


# ============================================================
# CONFIGURATION
# ============================================================

OLLAMA_URL = "http://127.0.0.1:11434/api/generate"
OLLAMA_MODEL = "qwen2.5:1.5b"

# Keep generation moderate because the model is running locally.
OLLAMA_NUM_PREDICT = 160

# Number of RAG documents to retrieve.
RAG_RESULTS = 2

# Maximum time allowed for Ollama to respond.
OLLAMA_TIMEOUT = 120

# Conservative Docker container memory signal.
# A container using >= 20% of its Docker memory limit is
# considered potentially significant.
SIGNIFICANT_CONTAINER_MEMORY_PERCENT = 20.0


# ============================================================
# UTILITY
# ============================================================

def now_iso():
    """Return the current local timestamp."""

    return datetime.now().isoformat()


# ============================================================
# SYSTEM TELEMETRY
# ============================================================

def get_system_telemetry():
    """
    Collect live host-level telemetry.

    This is observational data.
    It is not generated by the AI model.
    """

    memory = psutil.virtual_memory()
    disk = psutil.disk_usage("/")

    return {
        "platform": platform.system(),
        "kernel": platform.release(),

        "cpu": {
            "usage_percent": psutil.cpu_percent(interval=0.3),
            "cores": psutil.cpu_count() or 0,
        },

        "memory": {
            "total_gb": round(
                memory.total / (1024 ** 3),
                2,
            ),
            "used_gb": round(
                memory.used / (1024 ** 3),
                2,
            ),
            "available_gb": round(
                memory.available / (1024 ** 3),
                2,
            ),
            "usage_percent": memory.percent,
        },

        "disk": {
            "total_gb": round(
                disk.total / (1024 ** 3),
                2,
            ),
            "used_gb": round(
                disk.used / (1024 ** 3),
                2,
            ),
            "free_gb": round(
                disk.free / (1024 ** 3),
                2,
            ),
            "usage_percent": disk.percent,
        },
    }


# ============================================================
# DIGITAL TWIN
# ============================================================

def get_digital_twin():
    """
    Build a point-in-time representation of the
    developer environment.

    The Digital Twin combines:

    - host telemetry
    - Docker telemetry
    """

    return {
        "timestamp": now_iso(),
        "system": get_system_telemetry(),
        "docker": get_docker_metrics(),
    }


# ============================================================
# MEMORY PARSING
# ============================================================

def parse_memory_percentage(value):
    """
    Convert Docker memory percentage strings such as:

        '0.26%'

    into a float.

    Invalid values safely become 0.0.
    """

    if value is None:
        return 0.0

    try:
        return float(
            str(value)
            .replace("%", "")
            .strip()
        )

    except (ValueError, TypeError):
        return 0.0


# ============================================================
# DETERMINISTIC DIAGNOSIS
# ============================================================

def build_diagnosis(digital_twin):
    """
    Produce a deterministic diagnosis from telemetry.

    IMPORTANT:

    This function runs BEFORE the LLM.

    The LLM explains the diagnosis.
    The LLM does not determine the diagnosis.
    """

    system = digital_twin.get("system", {})
    memory = system.get("memory", {})
    docker = digital_twin.get("docker", {})

    host_memory_usage = memory.get(
        "usage_percent",
        0,
    )

    # --------------------------------------------------------
    # Docker unavailable
    # --------------------------------------------------------

    if not docker.get("available", False):

        return {
            "docker_status": "unavailable",

            "running_containers": None,

            "docker_container_memory": "unknown",

            "host_memory_usage_percent":
                host_memory_usage,

            "docker_is_evidently_responsible":
                "unknown",

            "assessment":
                "unknown",

            "significant_containers":
                [],

            "reason": (
                "Docker telemetry is unavailable. "
                "Docker container memory usage cannot "
                "currently be evaluated."
            ),
        }

    # --------------------------------------------------------
    # Docker available
    # --------------------------------------------------------

    containers = docker.get(
        "containers",
        [],
    )

    container_count = docker.get(
        "container_count",
        len(containers),
    )

    # --------------------------------------------------------
    # No containers
    # --------------------------------------------------------

    if container_count == 0:

        return {
            "docker_status": "available",

            "running_containers": 0,

            "docker_container_memory": "none",

            "host_memory_usage_percent":
                host_memory_usage,

            "docker_is_evidently_responsible":
                False,

            "assessment":
                "not_responsible",

            "significant_containers":
                [],

            "reason": (
                "No Docker containers are currently "
                "running, so the current host memory "
                "usage cannot be attributed to running "
                "Docker containers."
            ),
        }

    # --------------------------------------------------------
    # Containers exist
    # --------------------------------------------------------

    container_memory = []
    significant_containers = []

    for container in containers:

        name = container.get(
            "name",
            "unknown",
        )

        memory_usage = container.get(
            "memory_usage",
            "unknown",
        )

        memory_percent_raw = container.get(
            "memory_percent",
            "0%",
        )

        memory_percent = parse_memory_percentage(
            memory_percent_raw
        )

        container_info = {
            "name": name,
            "memory_usage": memory_usage,
            "memory_percent": memory_percent_raw,
        }

        container_memory.append(
            container_info
        )

        # ----------------------------------------------------
        # Conservative container signal
        # ----------------------------------------------------

        if (
            memory_percent
            >= SIGNIFICANT_CONTAINER_MEMORY_PERCENT
        ):

            significant_containers.append(
                {
                    "name": name,
                    "memory_percent": memory_percent,
                }
            )

    # --------------------------------------------------------
    # Significant container utilization
    # --------------------------------------------------------

    if significant_containers:

        return {
            "docker_status": "available",

            "running_containers":
                container_count,

            "docker_container_memory":
                container_memory,

            "host_memory_usage_percent":
                host_memory_usage,

            "docker_is_evidently_responsible":
                "potentially",

            "assessment":
                "potentially_significant",

            "significant_containers":
                significant_containers,

            "reason": (
                "One or more Docker containers show "
                "comparatively high memory utilization "
                "relative to their configured Docker "
                "memory limit. Docker may be contributing "
                "to resource pressure, but the available "
                "telemetry does not prove that Docker is "
                "the sole cause of host memory usage."
            ),
        }

    # --------------------------------------------------------
    # Containers running but low utilization
    # --------------------------------------------------------

    return {
        "docker_status": "available",

        "running_containers":
            container_count,

        "docker_container_memory":
            container_memory,

        "host_memory_usage_percent":
            host_memory_usage,

        "docker_is_evidently_responsible":
            False,

        "assessment":
            "unlikely",

        "significant_containers":
            [],

        "reason": (
            "Docker containers are running, but their "
            "observed memory utilization is low. Docker "
            "is therefore unlikely to be the primary "
            "cause of the current host memory usage."
        ),
    }


# ============================================================
# RAG CONTEXT
# ============================================================

def get_knowledge(question):
    """
    Retrieve technical knowledge relevant to the user's
    question.
    """

    try:

        documents = retrieve_context(
            question,
            number_of_results=RAG_RESULTS,
        )

        if not documents:
            return []

        return documents

    except Exception as exc:

        print(
            f"RAG retrieval error: {exc}"
        )

        return []


# ============================================================
# PROMPT BUILDER
# ============================================================

def build_prompt(
    question,
    digital_twin,
    diagnosis,
    documents,
):
    """
    Build a grounded prompt.

    Telemetry and deterministic diagnosis are explicitly
    authoritative.
    """

    context = (
        "\n\n".join(documents)
        if documents
        else "No relevant knowledge was retrieved."
    )

    telemetry_json = json.dumps(
        digital_twin,
        indent=2,
    )

    diagnosis_json = json.dumps(
        diagnosis,
        indent=2,
    )

    return f"""
You are DevPulse Nexus.

You are an AI-powered developer environment
observability and AIDevOps intelligence assistant.

Your responsibility is to explain the current
developer environment using:

1. LIVE DIGITAL TWIN TELEMETRY
2. DETERMINISTIC DIAGNOSIS
3. RETRIEVED TECHNICAL KNOWLEDGE

============================================================
CRITICAL RULES
============================================================

The LIVE TELEMETRY is authoritative.

The DETERMINISTIC DIAGNOSIS is authoritative.

The retrieved knowledge is supporting technical context.

You MUST NOT contradict the deterministic diagnosis.

You MUST NOT invent:

- containers
- processes
- memory values
- CPU values
- disk values
- resource limits
- errors
- causes
- configuration
- workloads

If the telemetry does not prove a cause, explicitly say:

"The exact cause cannot be determined from the
available telemetry."

Do not present speculation as fact.

Do not automatically blame Docker for host memory usage.

If running_containers is 0:

Docker containers are NOT responsible for the observed
host memory usage.

If containers are running but their observed memory
utilization is low:

Explain that Docker is unlikely to be the primary cause.

If the deterministic diagnosis says Docker may be
contributing:

Explain that Docker may be contributing, while making
clear that the telemetry does not prove sole causation.

IMPORTANT:

Do not describe ordinary host memory utilization as
a problem unless the telemetry or user question provides
evidence that it is problematic.

Always distinguish:

OBSERVED FACTS

from

LIKELY / POSSIBLE CAUSES.

============================================================
LIVE DIGITAL TWIN
============================================================

{telemetry_json}

============================================================
DETERMINISTIC DIAGNOSIS
============================================================

{diagnosis_json}

============================================================
RETRIEVED TECHNICAL KNOWLEDGE
============================================================

{context}

============================================================
USER QUESTION
============================================================

{question}

============================================================
RESPONSE FORMAT
============================================================

OBSERVED:

- Describe only relevant live telemetry.
- Mention Docker/container state when relevant.

LIKELY CAUSE:

- Explain what the telemetry supports.
- Do not invent a cause.
- If the exact cause is unknown, say so.
- Do not blame Docker without telemetry evidence.

RECOMMENDATION:

- Give practical troubleshooting steps.
- Use retrieved knowledge where relevant.
- Do not recommend actions unsupported by the
  available information.

Keep the response concise and technically useful.

Never contradict the deterministic diagnosis.
"""


# ============================================================
# OLLAMA
# ============================================================

def ask_ollama(prompt):
    """
    Send the grounded prompt to the local Ollama model.

    Returns:

        answer
        latency_seconds
    """

    start = time.perf_counter()

    response = requests.post(
        OLLAMA_URL,

        json={
            "model": OLLAMA_MODEL,

            "prompt": prompt,

            "stream": False,

            "options": {
                "num_predict":
                    OLLAMA_NUM_PREDICT
            },
        },

        timeout=OLLAMA_TIMEOUT,
    )

    response.raise_for_status()

    result = response.json()

    latency = round(
        time.perf_counter() - start,
        2,
    )

    answer = result.get(
        "response",
        "",
    ).strip()

    if not answer:

        answer = (
            "The local AI model returned an empty response."
        )

    return answer, latency


# ============================================================
# HOME
# ============================================================

@app.get(
    "/",
    response_class=HTMLResponse,
)
def home(request: Request):

    return templates.TemplateResponse(
        request=request,
        name="index.html",
        context={},
    )


# ============================================================
# HEALTH CHECK
# ============================================================

@app.get("/health")
def health():

    return {
        "status": "ok",
        "service": "DevPulse Nexus",
        "version": "1.0.0",
        "ollama_model": OLLAMA_MODEL,
    }


# ============================================================
# DIGITAL TWIN API
# ============================================================

@app.get("/digital-twin")
def digital_twin():

    snapshot = get_digital_twin()

    # Every Digital Twin request becomes a history point.
    record_telemetry(snapshot)

    return snapshot


# ============================================================
# TELEMETRY API
# ============================================================

@app.get("/telemetry")
def telemetry():

    return get_system_telemetry()


# ============================================================
# DOCKER API
# ============================================================

@app.get("/docker")
def docker():

    return get_docker_metrics()


# ============================================================
# HISTORY API
# ============================================================

@app.get("/history")
def history(limit: int = 120):
    """
    Return previously recorded Digital Twin snapshots.

    A fresh snapshot is NOT created here.
    This prevents /history from unexpectedly changing
    the telemetry history.
    """

    records = get_history(limit)

    return {
        "count": len(records),
        "records": records,
    }


# ============================================================
# DIAGNOSIS API
# ============================================================

@app.get("/diagnosis")
def diagnosis():

    twin = get_digital_twin()

    result = build_diagnosis(twin)

    return {
        "timestamp": twin["timestamp"],
        "diagnosis": result,
        "digital_twin": twin,
    }


# ============================================================
# ASK API
# ============================================================

@app.get("/ask")
def ask(question: str):

    question = question.strip()

    # --------------------------------------------------------
    # Validate question
    # --------------------------------------------------------

    if not question:

        return {
            "question": question,

            "answer": (
                "Please provide a developer or DevOps "
                "question."
            ),

            "diagnosis": None,

            "digital_twin": None,

            "sources": [],

            "ai": {
                "model": OLLAMA_MODEL,
                "latency_seconds": None,
                "error": None,
            },
        }

    # --------------------------------------------------------
    # 1. Collect live environment telemetry
    # --------------------------------------------------------

    digital_twin = get_digital_twin()

    # Record the snapshot because /ask is also an
    # observability event.
    record_telemetry(digital_twin)

    # --------------------------------------------------------
    # 2. Produce deterministic diagnosis
    # --------------------------------------------------------

    diagnosis = build_diagnosis(
        digital_twin
    )

    # --------------------------------------------------------
    # 3. Orchestrate application services
    # --------------------------------------------------------

    orchestration = orchestrate_request(
        question=question,
        digital_twin=digital_twin,
        diagnosis=diagnosis,
        rag_results=RAG_RESULTS,
    )

    documents = orchestration["retrieved_context"]

    # --------------------------------------------------------
    # 4. Build grounded prompt
    # --------------------------------------------------------

    prompt = build_prompt(
        question=question,
        digital_twin=digital_twin,
        diagnosis=diagnosis,
        documents=documents,
    )

    # --------------------------------------------------------
    # 5. Ask local AI model
    # --------------------------------------------------------

    ai_error = None
    latency = None

    try:

        answer, latency = ask_ollama(
            prompt
        )

    except requests.exceptions.Timeout:

        ai_error = (
            "The local AI model timed out. "
            "Live telemetry and deterministic diagnosis "
            "are still available."
        )

        answer = (
            "The local AI model took too long to respond. "
            "Review the returned diagnosis and Digital Twin "
            "telemetry for the current environment state."
        )

    except requests.exceptions.ConnectionError:

        ai_error = (
            "Could not connect to Ollama at "
            f"{OLLAMA_URL}."
        )

        answer = (
            "The local AI model is unavailable. "
            "Make sure Ollama is running and the configured "
            f"model '{OLLAMA_MODEL}' is available."
        )

    except requests.exceptions.HTTPError as exc:

        ai_error = str(exc)

        answer = (
            "Ollama returned an HTTP error while processing "
            "the request. The deterministic diagnosis and "
            "Digital Twin telemetry are still available."
        )

    except requests.exceptions.RequestException as exc:

        ai_error = str(exc)

        answer = (
            "The local AI model could not process the request. "
            "The deterministic diagnosis and Digital Twin "
            "telemetry are still available."
        )

    except Exception as exc:

        ai_error = str(exc)

        print(
            f"Ollama unexpected error: {exc}"
        )

        answer = (
            "An unexpected error occurred while generating "
            "the AI response. The deterministic diagnosis "
            "and Digital Twin telemetry are still available."
        )

    # --------------------------------------------------------
    # 6. Return complete observability response
    # --------------------------------------------------------

    return {
        "question": question,

        "answer": answer,

        "diagnosis": diagnosis,

        "digital_twin": digital_twin,

        "sources": documents,

        "ai": {
            "model": OLLAMA_MODEL,
            "latency_seconds": latency,
            "error": ai_error,
        },
    }


================================================================================
FILE: app/digital_twin.py
================================================================================
import psutil
import platform
import json
from datetime import datetime

from app.docker_monitor import get_docker_metrics


def get_system_telemetry():

    memory = psutil.virtual_memory()
    disk = psutil.disk_usage("/")

    return {
        "platform": platform.system(),
        "kernel": platform.release(),

        "cpu": {
            "usage_percent": psutil.cpu_percent(interval=0.5),
            "cores": psutil.cpu_count()
        },

        "memory": {
            "total_gb": round(memory.total / (1024 ** 3), 2),
            "used_gb": round(memory.used / (1024 ** 3), 2),
            "available_gb": round(memory.available / (1024 ** 3), 2),
            "usage_percent": memory.percent
        },

        "disk": {
            "total_gb": round(disk.total / (1024 ** 3), 2),
            "used_gb": round(disk.used / (1024 ** 3), 2),
            "free_gb": round(disk.free / (1024 ** 3), 2),
            "usage_percent": disk.percent
        }
    }


def get_digital_twin():

    return {
        "timestamp": datetime.now().isoformat(),

        "system": get_system_telemetry(),

        "docker": get_docker_metrics()
    }


if __name__ == "__main__":

    twin = get_digital_twin()

    print(json.dumps(twin, indent=2))


================================================================================
FILE: app/orchestrator.py
================================================================================
import json

from app.rag import retrieve_context


def orchestrate_request(
    question,
    digital_twin,
    diagnosis,
    rag_results=2,
):
    """
    Coordinate the major DevPulse Nexus services.

    Flow:
    User Question
        -> RAG Retrieval
        -> Context
        -> LLM-ready request

    Telemetry and diagnosis are supplied by the
    application service because they are authoritative.
    """

    documents = retrieve_context(
        question,
        number_of_results=rag_results,
    )

    context = (
        "\n\n".join(documents)
        if documents
        else "No relevant knowledge was retrieved."
    )

    return {
        "question": question,

        "services": {
            "application_service": "FastAPI",
            "orchestration_service": "DevPulse Orchestrator",
            "retrieval_service": "RAG + ChromaDB",
            "knowledge_service": "knowledge/docker.md",
            "embedding_service": "all-MiniLM-L6-v2",
            "llm_service": "Ollama",
            "llm_model": "qwen2.5:1.5b",
        },

        "pipeline": [
            "User Question",
            "Application Service",
            "Orchestration Service",
            "RAG Retrieval Service",
            "Relevant Context",
            "LLM Service",
            "Ollama",
            "Qwen 2.5 1.5B",
            "Response",
        ],

        "digital_twin": digital_twin,

        "diagnosis": diagnosis,

        "retrieved_context": documents,

        "llm_context": context,
    }


================================================================================
FILE: app/rag.py
================================================================================
import chromadb

from sentence_transformers import SentenceTransformer


DB_PATH = "./chroma_db"
MODEL_NAME = "all-MiniLM-L6-v2"


client = chromadb.PersistentClient(path=DB_PATH)

collection = client.get_collection(
    name="devpulse_knowledge"
)

model = SentenceTransformer(MODEL_NAME)


def retrieve_context(question, number_of_results=2):

    question_embedding = model.encode(
        [question]
    )[0].tolist()

    results = collection.query(
        query_embeddings=[question_embedding],
        n_results=number_of_results
    )

    documents = results["documents"][0]

    return documents


if __name__ == "__main__":

    question = "How can I investigate high Docker memory usage?"

    documents = retrieve_context(question)

    print("\nQuestion:")
    print(question)

    print("\nRetrieved knowledge:")

    for i, document in enumerate(documents, start=1):

        print("\n" + "=" * 60)
        print(f"RESULT {i}")
        print("=" * 60)
        print(document)


================================================================================
FILE: app/vector_store.py
================================================================================
import chromadb

from chunking import load_document, chunk_text
from embeddings import create_embeddings


DB_PATH = "./chroma_db"


def build_vector_store():

    client = chromadb.PersistentClient(
        path=DB_PATH
    )

    collection = client.get_or_create_collection(
        name="devpulse_knowledge"
    )

    file_path = "knowledge/docker.md"

    text = load_document(file_path)
    chunks = chunk_text(text)

    embeddings = create_embeddings(chunks)

    ids = [
        f"docker_chunk_{i}"
        for i in range(len(chunks))
    ]

    collection.upsert(
        ids=ids,
        documents=chunks,
        embeddings=embeddings.tolist(),
        metadatas=[
            {
                "source": file_path,
                "chunk": i
            }
            for i in range(len(chunks))
        ]
    )

    print("Vector database created successfully!")
    print(f"Collection: {collection.name}")
    print(f"Documents stored: {collection.count()}")


if __name__ == "__main__":
    build_vector_store()


================================================================================
FILE: app/embeddings.py
================================================================================
from sentence_transformers import SentenceTransformer

from chunking import load_document, chunk_text


MODEL_NAME = "all-MiniLM-L6-v2"


def create_embeddings(chunks):
    model = SentenceTransformer(MODEL_NAME)

    embeddings = model.encode(
        chunks,
        show_progress_bar=True
    )

    return embeddings


if __name__ == "__main__":

    file_path = "knowledge/docker.md"

    text = load_document(file_path)

    chunks = chunk_text(text)

    embeddings = create_embeddings(chunks)

    print("\nEmbedding complete!")
    print(f"Number of chunks: {len(chunks)}")
    print(f"Embedding dimensions: {embeddings.shape[1]}")

    for i, embedding in enumerate(embeddings, start=1):
        print(
            f"Chunk {i}: "
            f"{len(embedding)} dimensions"
        )


================================================================================
FILE: app/chunking.py
================================================================================
from pathlib import Path


def load_document(file_path):
    return Path(file_path).read_text(encoding="utf-8")


def chunk_text(text, chunk_size=500, overlap=100):
    chunks = []

    start = 0

    while start < len(text):
        end = start + chunk_size
        chunk = text[start:end]

        if chunk.strip():
            chunks.append(chunk.strip())

        start += chunk_size - overlap

    return chunks


if __name__ == "__main__":
    file_path = "knowledge/docker.md"

    text = load_document(file_path)

    chunks = chunk_text(text)

    print(f"Document length: {len(text)} characters")
    print(f"Number of chunks: {len(chunks)}")

    for i, chunk in enumerate(chunks, start=1):
        print("\n" + "=" * 60)
        print(f"CHUNK {i}")
        print("=" * 60)
        print(chunk)


================================================================================
FILE: app/docker_monitor.py
================================================================================
import subprocess
import json


def get_docker_metrics():

    try:
        result = subprocess.run(
            [
                "docker",
                "stats",
                "--no-stream",
                "--format",
                "{{json .}}"
            ],
            capture_output=True,
            text=True,
            timeout=10
        )

        if result.returncode != 0:
            return {
                "available": False,
                "error": result.stderr.strip()
            }

        containers = []

        for line in result.stdout.splitlines():

            if not line.strip():
                continue

            data = json.loads(line)

            containers.append({
                "name": data.get("Name"),
                "cpu_percent": data.get("CPUPerc"),
                "memory_usage": data.get("MemUsage"),
                "memory_percent": data.get("MemPerc"),
                "network_io": data.get("NetIO"),
                "block_io": data.get("BlockIO"),
                "pids": data.get("PIDs")
            })

        return {
            "available": True,
            "container_count": len(containers),
            "containers": containers
        }

    except FileNotFoundError:

        return {
            "available": False,
            "error": "Docker command not found"
        }

    except subprocess.TimeoutExpired:

        return {
            "available": False,
            "error": "Docker stats timed out"
        }


if __name__ == "__main__":

    print(
        json.dumps(
            get_docker_metrics(),
            indent=2
        )
    )


================================================================================
FILE: app/telemetry.py
================================================================================
import psutil
import platform
from datetime import datetime


def get_system_metrics():

    memory = psutil.virtual_memory()
    disk = psutil.disk_usage("/")

    processes = []

    for process in psutil.process_iter(
        ["pid", "name", "cpu_percent", "memory_percent"]
    ):
        try:
            info = process.info

            processes.append({
                "pid": info["pid"],
                "name": info["name"],
                "cpu_percent": info["cpu_percent"],
                "memory_percent": info["memory_percent"]
            })

        except (psutil.NoSuchProcess, psutil.AccessDenied):
            pass

    # Highest CPU processes
    top_cpu = sorted(
        processes,
        key=lambda x: x["cpu_percent"],
        reverse=True
    )[:5]

    # Highest memory processes
    top_memory = sorted(
        processes,
        key=lambda x: x["memory_percent"],
        reverse=True
    )[:5]

    return {
        "timestamp": datetime.now().isoformat(),

        "system": {
            "platform": platform.system(),
            "release": platform.release(),
            "machine": platform.machine()
        },

        "cpu": {
            "usage_percent": psutil.cpu_percent(interval=0.5),
            "cores": psutil.cpu_count()
        },

        "memory": {
            "total_gb": round(memory.total / (1024 ** 3), 2),
            "used_gb": round(memory.used / (1024 ** 3), 2),
            "available_gb": round(memory.available / (1024 ** 3), 2),
            "usage_percent": memory.percent
        },

        "disk": {
            "total_gb": round(disk.total / (1024 ** 3), 2),
            "used_gb": round(disk.used / (1024 ** 3), 2),
            "free_gb": round(disk.free / (1024 ** 3), 2),
            "usage_percent": disk.percent
        },

        "top_cpu_processes": top_cpu,
        "top_memory_processes": top_memory
    }


if __name__ == "__main__":

    import json

    metrics = get_system_metrics()

    print(json.dumps(metrics, indent=2))


================================================================================
FILE: app/telemetry_history.py
================================================================================
import json
import threading
from collections import deque
from datetime import datetime
from pathlib import Path


HISTORY_FILE = Path(__file__).resolve().parent.parent / "telemetry_history.json"

MAX_RECORDS = 720

_history = deque(maxlen=MAX_RECORDS)
_lock = threading.Lock()


def _load_history():
    if not HISTORY_FILE.exists():
        return

    try:
        with open(HISTORY_FILE, "r", encoding="utf-8") as file:
            records = json.load(file)

        if isinstance(records, list):
            with _lock:
                _history.extend(records[-MAX_RECORDS:])

    except (OSError, json.JSONDecodeError):
        pass


def _save_history():
    try:
        temporary_file = HISTORY_FILE.with_suffix(".tmp")

        with open(temporary_file, "w", encoding="utf-8") as file:
            json.dump(list(_history), file, indent=2)

        temporary_file.replace(HISTORY_FILE)

    except OSError:
        pass


_load_history()


def record_telemetry(digital_twin):
    record = {
        "timestamp": digital_twin.get(
            "timestamp",
            datetime.now().isoformat()
        ),
        "system": digital_twin.get("system", {}),
        "docker": digital_twin.get("docker", {})
    }

    with _lock:
        _history.append(record)
        _save_history()

    return record


def get_history(limit=120):
    try:
        limit = int(limit)
    except (TypeError, ValueError):
        limit = 120

    limit = max(1, min(limit, MAX_RECORDS))

    with _lock:
        return list(_history)[-limit:]


def clear_history():
    with _lock:
        _history.clear()

        try:
            if HISTORY_FILE.exists():
                HISTORY_FILE.unlink()
        except OSError:
            pass


if __name__ == "__main__":
    print(
        json.dumps(
            {
                "records": len(get_history()),
                "file": str(HISTORY_FILE)
            },
            indent=2
        )
    )


================================================================================
FILE: knowledge/docker.md
================================================================================
# Docker Memory Troubleshooting

## Container Memory Usage

Docker containers share the host Linux kernel. The memory consumed by a container depends largely on the application and processes running inside it.

A container can consume significant memory when the application itself requires a large amount of memory, when memory is not released efficiently, or when workloads create temporary memory spikes.

## Monitoring Container Memory

Docker provides commands for monitoring container resource usage.

The command:

docker stats

shows live CPU, memory, network, and block I/O statistics for running containers.

The memory usage of a particular container should be compared with its expected workload and configured resource limits.

## Memory Limits

Docker allows administrators to configure memory limits for containers.

For example:

docker run --memory=512m IMAGE

limits the container's memory allocation according to the configured Docker memory constraints.

Resource limits should be selected based on the application's actual requirements.

## Troubleshooting High Memory Usage

When a container shows unexpectedly high memory usage:

1. Identify the container using excessive memory.
2. Inspect its application processes.
3. Check recent application or configuration changes.
4. Examine container logs.
5. Compare current memory usage with historical behavior.
6. Check whether appropriate memory limits are configured.
7. Investigate possible application-level memory leaks.


================================================================================
FILE: evaluation/questions.json
================================================================================
[
  {"id":1,"category":"Explanation","question":"What does the get_digital_twin() function do?"},
  {"id":2,"category":"Explanation","question":"Explain how DevPulse collects host memory and CPU information."},
  {"id":3,"category":"Explanation","question":"Explain the purpose of the deterministic diagnosis."},
  {"id":4,"category":"Explanation","question":"Explain the role of the DevPulse orchestrator."},

  {"id":5,"category":"Code Retrieval","question":"Which file retrieves knowledge from ChromaDB?"},
  {"id":6,"category":"Code Retrieval","question":"Which file collects Docker container statistics?"},
  {"id":7,"category":"Code Retrieval","question":"Where is the Ollama model configured?"},
  {"id":8,"category":"Code Retrieval","question":"Which file stores telemetry history?"},

  {"id":9,"category":"Dependency Understanding","question":"How does /ask connect FastAPI, RAG, diagnosis, and Ollama?"},
  {"id":10,"category":"Dependency Understanding","question":"What components are involved between a user question and the final answer?"},
  {"id":11,"category":"Dependency Understanding","question":"Which module does orchestrate_request() depend on for knowledge retrieval?"},
  {"id":12,"category":"Dependency Understanding","question":"What happens to retrieved documents before they reach Ollama?"},

  {"id":13,"category":"Bug Analysis","question":"What happens if Docker is not installed or unavailable?"},
  {"id":14,"category":"Bug Analysis","question":"What happens if Ollama times out?"},
  {"id":15,"category":"Bug Analysis","question":"What could cause RAG retrieval to return no documents?"},
  {"id":16,"category":"Bug Analysis","question":"How does the code prevent Docker from being incorrectly blamed for high host memory usage?"},

  {"id":17,"category":"Code Generation","question":"Write Python code to calculate the average container memory percentage."},
  {"id":18,"category":"Code Generation","question":"Write a function to identify containers using more than 50% memory."},
  {"id":19,"category":"Code Generation","question":"Write a test for parse_memory_percentage()."},
  {"id":20,"category":"Code Generation","question":"Write a test verifying that zero running containers produces not_responsible."},
  {"id":21,"category":"Code Generation","question":"Write a FastAPI endpoint that returns the latest telemetry history record."},

  {"id":22,"category":"Refactoring","question":"How could build_diagnosis() be refactored to make it easier to test?"},
  {"id":23,"category":"Refactoring","question":"How could the Ollama configuration be moved from hardcoded values to environment variables?"},
  {"id":24,"category":"Refactoring","question":"How could duplicate Digital Twin logic between main.py and digital_twin.py be reduced?"},
  {"id":25,"category":"Refactoring","question":"Suggest an improvement to the error handling in orchestrator.py."},

  {"id":26,"category":"RAG-based Question","question":"According to the Docker knowledge base, what command shows live container resource usage?"},
  {"id":27,"category":"RAG-based Question","question":"According to the knowledge base, how should high Docker memory usage be investigated?"},
  {"id":28,"category":"RAG-based Question","question":"What Docker option can be used to configure a container memory limit?"},
  {"id":29,"category":"RAG-based Question","question":"What does the knowledge base recommend checking when a container unexpectedly uses high memory?"},
  {"id":30,"category":"RAG-based Question","question":"Does the retrieved knowledge say that Docker is always responsible for high host memory usage? Explain."}
]



30 EVALUATION QUESTIONS
======================================================================

Q01 [Explanation]
What does the get_digital_twin() function do?

Q02 [Explanation]
Explain how DevPulse collects host memory and CPU information.

Q03 [Explanation]
Explain the purpose of the deterministic diagnosis.

Q04 [Explanation]
Explain the role of the DevPulse orchestrator.

Q05 [Code Retrieval]
Which file retrieves knowledge from ChromaDB?

Q06 [Code Retrieval]
Which file collects Docker container statistics?

Q07 [Code Retrieval]
Where is the Ollama model configured?

Q08 [Code Retrieval]
Which file stores telemetry history?

Q09 [Dependency Understanding]
How does /ask connect FastAPI, RAG, diagnosis, and Ollama?

Q10 [Dependency Understanding]
What components are involved between a user question and the final answer?

Q11 [Dependency Understanding]
Which module does orchestrate_request() depend on for knowledge retrieval?

Q12 [Dependency Understanding]
What happens to retrieved documents before they reach Ollama?

Q13 [Bug Analysis]
What happens if Docker is not installed or unavailable?

Q14 [Bug Analysis]
What happens if Ollama times out?

Q15 [Bug Analysis]
What could cause RAG retrieval to return no documents?

Q16 [Bug Analysis]
How does the code prevent Docker from being incorrectly blamed for high host memory usage?

Q17 [Code Generation]
Write Python code to calculate the average container memory percentage.

Q18 [Code Generation]
Write a function to identify containers using more than 50% memory.

Q19 [Code Generation]
Write a test for parse_memory_percentage().

Q20 [Code Generation]
Write a test verifying that zero running containers produces not_responsible.

Q21 [Code Generation]
Write a FastAPI endpoint that returns the latest telemetry history record.

Q22 [Refactoring]
How could build_diagnosis() be refactored to make it easier to test?

Q23 [Refactoring]
How could the Ollama configuration be moved from hardcoded values to environment variables?

Q24 [Refactoring]
How could duplicate Digital Twin logic between main.py and digital_twin.py be reduced?

Q25 [Refactoring]
Suggest an improvement to the error handling in orchestrator.py.

Q26 [RAG-based Question]
According to the Docker knowledge base, what command shows live container resource usage?

Q27 [RAG-based Question]
According to the knowledge base, how should high Docker memory usage be investigated?

Q28 [RAG-based Question]
What Docker option can be used to configure a container memory limit?

Q29 [RAG-based Question]
What does the knowledge base recommend checking when a container unexpectedly uses high memory?

Q30 [RAG-based Question]
Does the retrieved knowledge say that Docker is always responsible for high host memory usage? Explain.
